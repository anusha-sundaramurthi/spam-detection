# Purpose: Keeps backend test discovery scoped to maintained tests and ignores
# temporary pytest directories that Windows may leave with restricted access.
[pytest]
testpaths = tests
norecursedirs = pytest-of-* .pytest_cache .venv
