/**
 * Purpose: Configures Vite to compile and serve the React frontend.
 */
/**
 * Purpose: Configures Vite to compile and serve the React frontend.
 */
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({ plugins: [react()] });
